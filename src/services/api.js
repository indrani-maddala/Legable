/**
 * API client stub.
 * Replace mock calls with fetch() to a Node.js/Express backend later.
 * Example: GET /api/scans, POST /api/scans, Supabase client in a later step.
 */
export const API_BASE_URL =
  import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000/api';
